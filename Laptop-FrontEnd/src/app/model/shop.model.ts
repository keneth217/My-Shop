export interface ShopData {
  id: number;
  shopName: string;
  owner: string;
  adminUsername: string;
  phoneNumber: string;
  address: string;
  responseMessage: string; // Add this field to match the response structure
}